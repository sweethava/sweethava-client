export enum EFieldSizes {
    SMALL = 'small',
    SMALLER = 'smaller',
    DEFAULT = 'default',
    LARGER = 'larger',
    LARGE = 'large',
}

export enum EFieldStyles {
    PRIMARY = 'primary',
    GREY = 'grey',
    OUTLINE = 'outline',
}

export enum EFieldColors {
    PRIMARY = 'primary',
    RED = 'red',
    GREEN = 'green',
    BLUE = 'blue',
    ORANGE = 'orange',
}